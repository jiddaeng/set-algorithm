{
  "name": "set-algorithm",
  "version": "1.0.0",
  "private": true,
  "description": "Remote parental controls for the Set Algorithm Chrome extension.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check remote-server/server.js && node tests/classifier.test.js"
  }
}
