{
  "name": "set-algorithm",
  "version": "1.0.0",
  "private": true,
  "description": "Remote parental controls for the Set Algorithm Chrome extension.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check remote-server/server.js && node --check background.js && node --check popup.js && node --check remote-server/public/app.js && node tests/classifier.test.js && node tests/server.test.js"
  }
}
